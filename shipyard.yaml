apiVersion: "spec.keptn.sh/0.2.0"
kind: "Shipyard"
metadata:
  name: "shipyard-quality-gates"
spec:
  stages:
    - name: "quality-gate"
      sequences:
        - name: "evaluation"
          tasks:
            - name: "task1"
              properties:
                teststrategy: "performance"
            - name: "task2"
