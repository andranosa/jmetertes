apiVersion: "spec.keptn.sh/0.2.0"
kind: "Shipyard"
metadata:
  name: "shipyard-quality-gates"
spec:
  stages:
    - name: "quality-gate"
      tasks:
       - name: "evaluation"
       - name: "build"
       - name: "test"
         properties:
         teststrategy: "performance"


